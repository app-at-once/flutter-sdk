import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Smoke test - SDK can be imported', () {
    // This is a basic smoke test to ensure the SDK can be imported
    expect(1 + 1, equals(2));
  });
}
